#include <ctype.h>
#include <stdio.h>

/**
 *_isupper(int c): checks if a string passed is uppercase
 * return: 0 or 1
 */

_isupper(int c)
{
	if(c >= && c <= 90)
	{
		return(1);
	}else{
		return(0);
	}
}
