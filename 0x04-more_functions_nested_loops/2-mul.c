#include "main.h"
/**
* mul - multiplies two given parameters in number and returns the product
* @a: is the first integer
* @b: is the second integer
* Return: the result of the multiplication
*/

int mul(int a, int b)
{
	int result;

	result = a * b;
	return (result);
}
