#include <string.h>
#include "main.h"

/**
 * _strlen - accept a string wihch will be reversed
 * @s: is the string variable
 * Return: the length of the scting
 */

int _strlen(char *s)
{
	int string_length;

	string_length = strlen(*s);
	return (string_length);
}
